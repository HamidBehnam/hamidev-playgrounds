[project]
name = "first-llm-call"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.117.0",
    "pydantic>=2.13.4",
]

[dependency-groups]
dev = [
    "ruff>=0.15.22",
]
